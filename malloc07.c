/*
Aloque um vetor para 10 inteiros de forma dinâmica e inicialize os vetores com algum valor e
imprima na tela:
*/
#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
void f_d (int vet(10))

int main() {
   

    

    int i;
    for (i = 0; i < n; i++)
        v[i] = i * i;

    for (i = 0; i < n; i++)
        printf("v[%d] = %d\n", i, v[i]);

    free(v);

    return 0;
}
